spring:
  cloud:
    gateway:
      mvc:
        routes:

          - id: auth-service
            uri: http://localhost:8081
            predicates:
              - Path=/auth/**

management:
  endpoints:
    web:
      exposure:
        include: "*"