spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_customer
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


server:
  port: 8086

jwt:
  secret: MyVerySecretKeyForEcommerceApplicationJWTAuthentication2026
  expiration: 86400000